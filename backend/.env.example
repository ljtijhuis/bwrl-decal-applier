PORT=3001
NODE_ENV=production
# Comma-separated list of origins allowed to call the API (the league website domain(s))
ALLOWED_ORIGINS=https://www.brokenwingracingleague.com/,http://localhost:3000
# Rate limiting (requests per 15 minutes per IP)
RATE_LIMIT_GLOBAL=200
RATE_LIMIT_APPLY=20
